<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
    <!-- Wrapped in a link to /articles/:id -->
      <a href="/articles/<%= article.id %>">
        <%= article.title %>
      </a>
    </li>
  <% end %>
</ul>
